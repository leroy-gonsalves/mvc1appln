@model MVC_Auction.Models.Auction

@*@{
    var auction = new MVC_Auction.Models.Auction()
    {
        Title = "Sample Auction",
        Description = "this is auction",
        StartTime = DateTime.Now,
        EndTime = DateTime.Now.AddDays(7),
        StartPrice = 1.00m,
        CurrentPrice = null,
    };
}*@           @*never create model into view.create it in controller*@

@{
    //var auction = (MVC_Auction.Models.Auction)ViewData["Auction"];  //need to cast object for viewdata not strongly typed
    //var auction = ViewBag["Auction"]; // don't need to cast 
    var auction = Model;
}

@Html.ActionLink("Login","Login","Account")

<div class="auction">

    <h3>@auction.Title</h3>
<div class="details">   
    <p>Start Time: @auction.StartTime.ToString()</p>
    <p>End Time: @auction.EndTime.ToString()</p>
    <p>Start Price: @auction.StartPrice.ToString()</p>
    <p>CurrentPrice:
    @if (auction.CurrentPrice == null)
    {
        @: [No bids]     
    }
    else
    {
        @auction.CurrentPrice.ToString()
    }</p>
</div>
    @if (auction.ImageUrl != null)
    {
     <img src="@auction.ImageUrl" title="@auction.Title" />   
    }
    
    <div class="description">
        @auction.Description
    </div>
</div>